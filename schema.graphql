directive @isAuthenticated on FIELD | FIELD_DEFINITION
directive @hasScope(scope: [String]) on FIELD | FIELD_DEFINITION

type Query {
  version: String! @isAuthenticated
}

type Mutation {
  verifyIdToken(token:String!): Boolean!
}